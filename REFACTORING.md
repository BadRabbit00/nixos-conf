# План Рефакторинга: Проект "Bocchi the Rock" -> "Dynamic Aesthetic"

Этот документ описывает шаги по трансформации системы из статичной и тяжеловесной в динамичную, легкую и интеллектуальную.

## 1. Вход в систему (Display Manager)
*   **ЧТО СНОСИМ:** `SDDM`. Слишком громоздкий и сложный в кастомизации под современные стандарты.
*   **ЧТО СТАВИМ:** `greetd` + `ReGreet`.
*   **ЦЕЛЬ:** Чистый интерфейс логина на GTK, который легко подхватывает общие обои и темы. Кастомные поля ввода и шрифты.

## 2. Панель и Виджеты
*   **ЧТО СНОСИМ:** `eww`. Излишне сложная логика на `yuck` и нестабильность.
*   **ЧТО СТАВИМ:** `Waybar`.
*   **ЦЕЛЬ:** Стабильность, простота настройки через JSON/CSS. Полная интеграция с динамическими цветами.

## 3. Лаунчер приложений
*   **ЧТО СНОСИМ:** `fuzzel`. Слишком примитивен.
*   **ЧТО СТАВИМ:** `rofi-wayland`.
*   **ЦЕЛЬ:** Гибкие меню (приложения, питание, калькулятор), поддержка кастомных CSS-тем.

## 4. Обои и Визуал
*   **ЧТО СНОСИМ:** `mpvpaper` (видео-обои нахуй). Пожирание ресурсов и лишний шум.
*   **ЧТО СТАВИМ:** `swww`.
*   **ЦЕЛЬ:** Статичные изображения с плавными переходами (transitions) и минимальным потреблением CPU/RAM.

## 5. Динамическая палитра (The Core)
*   **ИНСТРУМЕНТЫ:** `matugen` (генерация схем) + `hyprpicker` (пипетка).
*   **МЕХАНИКА:**
    1.  Выбираем обои (или цвет пипеткой).
    2.  `matugen` анализирует цвета и генерирует переменные для CSS/Nix.
    3.  Система мгновенно обновляет цвета в Waybar, Hyprland, Kitty и Rofi без перезагрузки.

## Шаги реализации:
- [ ] Очистка `home/desktop/` от старых конфигов (eww, fuzzel).
- [ ] Настройка `swww` и базового скрипта смены обоев.
- [ ] Интеграция `matugen` для создания глобальных цветовых переменных.
- [ ] Написание конфига `Waybar` с поддержкой динамических цветов.
- [ ] Переход с `SDDM` на `greetd` в системных модулях.
- [ ] Настройка `rofi-wayland` и его стилизация.
